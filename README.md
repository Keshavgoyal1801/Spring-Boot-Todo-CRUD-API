# Spring-Boot-Todo-CRUD-API
A RESTful Todo List backend application built using Spring Boot. This project provides a simple API to perform CRUD (Create, Read, Update, Delete) operations on todo items.
